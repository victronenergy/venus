# tja, that looks a bit weird... but works
TCMODE = "external-raspberrypi"
TARGET_OS = "arm-linux-gnueabihf"
# lets use the host cross compiler
EXTERNAL_TOOLCHAIN = "/usr"
TARGET_SYS = "arm-linux-gnueabihf"
DISTRO = "debian-jessie"
MACHINE ?= "debian-armhf"
